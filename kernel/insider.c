inline insider(){
  char a,b,c,d,e,f;
  return 
  "a + b + c + d + e + f"
  "a - b - c - d - e - f"
  "a * b * c * d * e * f"
  "a / b / c / d / e / f"
  "a ^ b ^ c ^ d ^ e ^ f"
  "a % b % c % d % e % f"
;} 
inline inout(){
    char m,n,o,p,q,r;
    return 
    "m->n->o->p->q->r"
    "n->o->p->q->r->m"
    "o->p->q->r->m->n"
    "p->q->r->m->n->o"
    "q->r->m->n->o->p"
    "r->m->n->o->p->q"
;}