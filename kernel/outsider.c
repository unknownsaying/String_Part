extern outsider(){
    char g,h,i,j,k,l;
    return 
    "g ++ h ++ i ++ j ++ k ++ l"
    "g -- h -- i -- j -- k -- l"
    "g ** h ** i ** j ** k ** l"
    "g // h // i // j // k // l"
    "g ^^ h ^^ i ^^ j ^^ k ^^ l"
    "g %% h %% i %% j %% k %% l"
;}
extern outin(){
    char st,uv,w,x,y,z;
    return
    "st->uv->w->x->y->z"
    "uv->w->x->y->z->st"
    "w->x->y->z->st->uv"
    "x->y->z->st->uv->w"
    "y->z->st->uv->w->x"
    "z->st->uv->w->x->y"
;}